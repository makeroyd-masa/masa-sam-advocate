# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc

# Databases
# read-only reference data from the medical_billing_data repo — never commit
data/pilot.db
# writable case/state store — created from docs/app_schema_v0_1.sql
app.db
*.sqlite
*.sqlite3
# SQLite runtime sidecars (WAL / shared-memory / rollback journal)
*.db-shm
*.db-wal
*.db-journal
# ...but the tiny committed slim DB for dependency-free CI is an exception
!tests/fixtures/ci_pilot.db

# PHI — real member EOBs used as design evidence. NEVER commit (guardrail:
# "PHI stays in app.db only; no documents are stored"). Referenced by filename
# in docs/PRD_addendum_costshare_v0_1.md but kept out of version control.
docs/eob_table_example.png
docs/full_eob_example.pdf
docs/*_eob_*.pdf
docs/*_eob_*.png

# Env / secrets
.env
.env.local

# Python / test caches
.pytest_cache/
.ruff_cache/
*.egg-info/

# Build output
dist/
build/
.vite/

# Claude Code personal overrides
.claude/settings.local.json

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
