"""SAM Medical Bill Advocate — application tier (FastAPI backend).

See docs/PRD.md (the spec) and CLAUDE.md (project instructions). The engine is a
deterministic, case-anchored guided flow; chat is only the interface skin.
"""

__version__ = "0.1.0"
