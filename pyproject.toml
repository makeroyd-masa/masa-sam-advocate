[project]
name = "masa-sam-advocate"
version = "0.1.0"
description = "SAM — member-facing guided medical-bill advocacy tool for MASA (application tier)."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
